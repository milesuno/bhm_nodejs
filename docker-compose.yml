version: "3.8"
services:
  my-service:
  build: .
  container_name: pcc0cc08080cg0go0kwokkw8
  environment:
    - OLLAMA_URL=http://ollama_container:11434 # Use container name for communication
  networks:
    - my_network

ollama_container:
  image: j8okgsso4wkoc04so004k8ws # Replace 'ollama_image' with the actual image name for your Ollama container
  container_name: 74627c3b10099d873a3a853d022d487dd53369c0b84ecd4c17a31f6ddf3620be # You can change this name if you prefer something else
  networks:
    - my_network # This ensures that your Ollama container is on the same network as your Node.js app

networks:
  my_network:
    driver: bridge
